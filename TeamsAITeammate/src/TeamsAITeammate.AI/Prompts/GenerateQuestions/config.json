{
  "schema": 1,
  "description": "Generate follow-up questions to extract tacit knowledge from meeting participants.",
  "execution_settings": {
    "default": {
      "max_tokens": 2048,
      "temperature": 0.5
    }
  },
  "input_variables": [
    { "name": "transcript", "description": "Formatted meeting transcript" },
    { "name": "meetingSubject", "description": "Meeting subject" },
    { "name": "participants", "description": "Comma-separated participant names" },
    { "name": "language", "description": "Detected language of the conversation" },
    { "name": "maxQuestions", "description": "Maximum number of questions to generate" },
    { "name": "alreadyAskedQuestions", "description": "Questions already asked to avoid duplicates" },
    { "name": "preferredTypes", "description": "Preferred question types" }
  ]
}
