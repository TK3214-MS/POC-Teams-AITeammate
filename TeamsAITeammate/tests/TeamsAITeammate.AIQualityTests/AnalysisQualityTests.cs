namespace TeamsAITeammate.AIQualityTests;

public class AnalysisQualityTests
{
    [Fact(Skip = "Requires Azure OpenAI connection")]
    public void Placeholder_AIQualityTestSetup()
    {
        // AI quality tests require a live Azure OpenAI connection
        Assert.True(true);
    }
}
